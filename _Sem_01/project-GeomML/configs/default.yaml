defaults:
  - _self_
  - model: egnn_gap
  - optimizer: adamw
  - scheduler: reduce_lr
  - dataset: qm9
  - train: default
  - checkpoint: default

train:
  batch_size: 32
  lr: 0.001
  epochs: 50

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d_%H-%M-%S}